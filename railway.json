{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "watchPatterns": ["app", ".env", "uv.lock", "railway.json"]
  },
  "deploy": {
    "startCommand": "uv run fastapi run --port $PORT",
    "restartPolicyType": "ALWAYS"
  }
}
