{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "watchPatterns": ["!tests", "!.github"]
  },
  "deploy": {
    "startCommand": "uv run fastapi run --port $PORT",
    "restartPolicyType": "ALWAYS"
  }
}
